ZequentMDBoxLayout:

<ZequentMDBoxLayout@MDBoxLayout>:
    orientation: "vertical"
    MDTopAppBar:
        title: "---"
        right_action_items:
            [
            ["home", lambda x: app.callback(x), "", "Home"],
            ["message-star", lambda x: app.callback(x), "", "Message star"],
            ["message-question", lambda x: app.callback(x), "" , "Message question"],
            ["message-reply", lambda x: app.callback(x), "", "Message reply"],
            ]
        left_action_items: 
            [
            ["menu", lambda x: app.callback_1()],
            ["zequent_logo.png", lambda x: app.callback_1()]
            ]
        mode: "end"
        md_bg_color: [1,0.398,0.796,1]
    MDBoxLayout:
        orientation: "horizontal"
        size_hint: 1,0.1
        md_bg_color: [1,0.398,0.796,1]
        Button:
            text: app.get_welcome_text()
            halign: "center"
        Button:
            text: app.get_welcome_text()
            halign: "center"
            background_color: [1,0.398,0.796,1]
    ScrollView:
        do_scroll_y: True
        MDGridLayout:
            size_hint_y: None
            orientation: "lr-tb"
            cols: 2 
            Button:        
                text: 'Hello 1'
                size_hint: 1,1
            Button:
                text: 'World 1'
                size_hint: 1,1
            Button:
                text: 'Hello 2'
                size_hint: 1,1
            Button:        
                text: 'World 2'
                size_hint: 1,1
            Button:        
                text: 'Hello 1'
                size_hint: 1,1
            Button:
                text: 'World 1'
                size_hint: 1,1
            Button:
                text: 'Hello 2'
                size_hint: 1,1
            Button:        
                text: 'World 2'
                size_hint: 1,1
        
    